# Copy or symlink to ~/.config/niketan/connect-secrets.sh and chmod 600.
# This file is not managed by niketan git — keep it only on your machines.
#
# If set, sshpass is used (e.g. brew install hudochenkov/sshpass/sshpass on macOS).
# Prefer ssh-copy-id / SSH keys and leave this empty in production.
CONNECT_SSH_PASSWORD=""
